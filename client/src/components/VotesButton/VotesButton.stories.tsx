import { VotesButton } from "./VotesButton";
import React from "react";

export default {
    title: "UI/Votes",
    component: VotesButton
}

export const Default = () => {
    return (
        <VotesButton />
    )
}